#include <bits/stdc++.h>
using namespace std;

// Brute force solution to the problem
int32_t main() {
    int32_t n; cin >> n;
    vector<int32_t> a(n);
    for(auto &x : a) 
        cin >> x;
}